# الدليل الإرشادي الصارم للذكاء الاصطناعي (AI Agent Core Rules)
**مشروع: المتتبع المالي الذكي (Smart Financial Tracker)**

> **تنبيه لأي وكيل ذكاء اصطناعي (AI Agent) يعمل على هذا المشروع:**
هذا المستند هو "الدستور البرمجي والفلسفي" للمشروع. يمنع منعاً باتاً كتابة أي كود أو اقتراح أي تصميم يخالف المبادئ المعمارية وتجربة المستخدم المذكورة هنا.

---

## 1. الفلسفة الأساسية (Core Philosophy)
1.  **Invisible Design (التصميم غير المرئي):** التطبيق يجب أن يعمل كمراقب صامت في الخلفية. لا تطلب من المستخدم إدخال بيانات يدوية أبداً إلا كخيار ثانوي (للكاش).
2.  **Zero-Touch Automation:** أي ميزة يجب أن تصمم لتعمل بأقل عدد ممكن من النقرات (Zero Clicks if possible).
3.  **Local-First & Privacy (الأولوية للمحلية):** يمنع كتابة أي كود يقوم بإرسال بيانات المستخدم المالية إلى خوادم خارجية للتحليل. كل عمليات المعالجة الـ (NLP/Regex) تتم 100% داخل مساحة التخزين الخاصة بالهاتف (Room DB).

## 2. القواعد المعمارية (Architectural Rules)
1.  **Config-Driven Development:** لا تقم بكتابة أي شروط تحليل (If-Else) أو تعبيرات نمطية (Regex) لمعالجة الرسائل البنكية داخل كود (Kotlin/Dart). يجب أن توضع جميع قواعد التحليل حصرياً في ملف `financial_tracker_config.json`.
2.  **Decoupled Modules:** حافظ على استقلالية موديول `financial_tracker` كلياً عن واجهة المستخدم (UI App). الموديول يحلل ويخزن ويبث (Emits)، والواجهة تستمع وتعرض فقط.
3.  **Reactive Streams (التدفق التفاعلي):** استخدم دائماً `SharedFlow` أو `StateFlow` في تمرير البيانات. **ممنوع استخدام Pull-to-Refresh**; الشاشات يجب أن تتحدث فوراً وبشكل آلي عند التقاط إشعار مالي في الخلفية (collectAsStateWithLifecycle).

## 3. قواعد واجهة المستخدم وتجربة العميل (UI/UX Guidelines)
1.  **الذكاء اللوني (Color Intelligence):**
    *   الأخضر الدائم: للواردات والمداخيل النقدية.
    *   الأحمر أو الدرجات الساخنة: للمصروفات، ويجب استخدام تدرج لوني (أخضر -> برتقالي -> أحمر) كلما اقترب المستخدم من السقف المالي.
    *   الأزرق: للفواتير والخدمات.
2.  **التصنيف البصري (Visual Categorization):** يجب استبدال النصوص الجامدة بأيقونات معبرة (مثال: "يمن نت" = 📶، "जوجل بلاي" = 🎮).
3.  **تبسيط الصلاحيات (Frictionless Onboarding):** يمنع طلب صلاحية (Notification Access) فور فتح التطبيق لأول مرة بشكل مفاجئ. يجب دائماً تقديم شاشة توضيحية (Privacy Anchor Card) تطمئن المستخدم أن بياناته لا تغادر هاتفه، قبل إظهار مربع طلب الصلاحية من النظام.

## 4. ميكانيكية النجاة وإدارة الأخطاء (Resilience & Error Handling)
1.  **The Fallback Trap (الإشعارات المجهولة):** في حال فشل الـ `Regex` في فهم رسالة بنكية، يمنع مسحها أو تجاهلها. يجب تسجيلها في جدول `UnparsedNotification` كرسالة خام.
2.  **الاسترداد الرجعي (Retroactive Reprocessing):** يجب أن تدعم الواجهة استدعاء دالة `reprocessUnparsedLogs()` لتمكين النظام من إعادة إحياء وتحليل الرسائل المجهولة بعد تحديث ملف القواعد (JSON).
3.  **إدارة بطارية النظام (Doze Mode):** تأكد دائماً من توجيه المستخدم لتعطيل "تحسين البطارية" (Ignore Battery Optimizations) لضمان استمرار عمل خدمة الاستماع في الخلفية.

## 5. الجانب الاجتماعي في الأموال (Social FinTech Ledger)
1.  **الديون والحوالات:** عند التقاط حوالة شخصية صادرة، يجب اقتراح "سياق النية" بسؤال تفاعلي سريع: (هل هذا سلف 🤝 أم سداد ↩️؟).
2.  **مواءمة الكاش والإلكتروني (Hybrid Drag & Drop):** في دفتر ديون أي شخص، يجب دمج الحوالات التلقائية والتسجيلات اليدوية في "جدول زمني موحد" (Unified Timeline)، مع دعم السحب والإسقاط لتسوية الديون.

---
**إقرار الذكاء الاصطناعي (AI Acknowledgement):**
بمجرد قراءة هذا الملف، يجب على الذكاء الاصطناعي تطبيق هذه المعايير بصرامة في أي اقتراح كود، تصميم، أو نقاش تقني يخص هذا المشروع، واعتبارها خطوطاً حمراء لا يجوز تجاوزها.
