# خطة تنفيذ الواجهات الكاملة - المتتبع المالي الذكي (The Complete UI Plan)

بناءً على "الفلسفة التصميمية" (Invisible Design & Zero-Touch Automation)، تم تقسيم إنجاز واجهات التطبيق (UI) إلى خمس مراحل استراتيجية متسلسلة. تم تنفيذها بالكامل باستخدام **Jetpack Compose** بأعلى معايير الأداء والـ Material 3.

## المرحلة الأولى: تأسيس المشروع (Project Setup)
**الهدف:** دمج الواجهات الجديدة (App Module) مع محرك معالجة الإشعارات (Financial Tracker Module).
1. **نقل המوديول:** نسخ مجلد `financial_tracker` بأكمله إلى قلب المشروع الجديد.
2. **الربط الداخلي:** تعديل ملفات `settings.gradle.kts` و `build.gradle.kts` لربط الموديول كمكتبة محلية (Local Dependency).
3. **تهيئة بيئة Jetpack Compose:** إضافة مكتبات الـ `lifecycle-runtime-compose` لتمكين جمع البيانات التلقائي (Reactive Flow).

## المرحلة الثانية: بناء الثقة والصلاحيات (Onboarding & Privacy)
**الهدف:** إقناع المستخدم بإعطاء صلاحيات الإشعارات دون إخافته.
- **`WelcomeScreen.kt`:** شاشة الترحيب. تحتوي على "مرساة الثقة" (Privacy Anchor Card)؛ وهي بطاقة تصميمية توضح أن العمل يتم %100 بدون إنترنت، محلياً على الهاتف.
- **`PermissionFlowScreen.kt`:** شاشة إرشادية (Frictionless) تأخذ المستخدم برحلة خطوة بخطوة للحصول على:
  - صلاحية الوصول للإشعارات (Notification Listener).
  - إيقاف قيود البطارية (Ignore Battery Optimization) لضمان عمل "المستشار الخفي" في الخلفية وعدم قتل النظام له.

## المرحلة الثالثة: المحافظ والتصنيف (Wallets Setup)
**الهدف:** تجهيز البنية البصرية للمحافظ والأيقونات.
- **`WalletsSetupScreen.kt`:** شبكة ذكية (Grid) تبرز "محفظة جيب" في حالة التفعيل الكامل (Active)، بينما تعرض المحافظ الأخرى (الكريمي، جوالي، إلخ) بشفافية وعلامة **"قريباً"** لخلق ترقب إيجابي (Anticipation Effect).
- **`CashWalletToggle.kt`:** مكون واجهة يسمح للمستخدم بتفعيل الميزانية النقدية اليدوية كجزء إضافي إلى جانب الإشعارات.
- **`CategoryMapper.kt`:** العقل المدبر لتلوين العمليات! كلاس وظيفته أخذ كلمة مفتاحية (مثل: يمن نت، جامعة، ببجي) وربطها آلياً بأيقونة (Icon) ولون (Color) مميز لكسر العمى المالي (Financial Blindness).

## المرحلة الرابعة: لوحة القيادة الحية (Reactive Dashboard)
**الهدف:** الشاشة الرئيسية ومصدر "اللحظة المدهشة" (The Aha! Moment).
- **`UnifiedDashboardScreen.kt`:** قلب التطبيق النابض. 
  - **لا يوجد سحب للتحديث (No Pull-to-Refresh):** بفضل معمارية `SharedFlow`، الشاشة تتحدث وترسم نفسها وتغير أرقام "الرصيد المتاح" والإيرادات والمصروفات فوراً لحظة وصول الإشعار في الخلفية وبدون أي لمس.
  - تستخدم الشاشة `CategoryMapper` لترجمة العمليات المعقدة إلى شكل بصري أنيق جداً (أيقونة ملونة + مبلغ بلون أخضر/أحمر + توقيت دقيق).

## المرحلة الخامسة: العلاقات المالية والصيانة (Social Ledger & Resilience)
**الهدف:** معالجة الديون ومعالجة أخطاء التعرف على الإشعارات.
- **`SocialLedgerScreen.kt`:** شاشة للديون تحتوي على "مؤشر التوازن الثنائي" (كم لك، وكم عليك). تقدم مطالبات ذكية للمستخدم عندما يقوم بحوالة غير مصنفة: "هل هذه الحوالة عبارة عن سلفة أم سداد لدين سابق؟".
- **`UnparsedLogsScreen.kt` (Admin Hub):** شاشة "الفخ الاحتياطي" للمطورين. عندما يصل إشعار بنكي لا يفهمه النظام، يُسجن هنا، ويقدم زراً بعنوان **(إعادة المعالجة - Reprocess)**. هذا يسمح بتحديث ملف הـ JSON للبنك لاحقاً واسترداد المعاملة المفقودة بأثر رجعي ودون إزعاج المستخدم.

---

> [!NOTE] 
> **حالة التنفيذ:** تم إنجاز جميع المراحل الخمس ومرتكزاتها البرمجية بالكامل ضمن حزمة `com.shehabgo.smartfinancialtracker.ui.screens`. المشروع الآن جاهز لخطوة (التوجيه - Navigation) وربط الشاشات داخل `MainActivity`.
